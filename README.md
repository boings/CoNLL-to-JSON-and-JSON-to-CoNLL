# JSONL to ConLL scripts

1. Reads JSONL files from directory, converts them to CoNLL format and outputs the converted CoNLL files in a new directory.
        Usage command line: python json_conll input_folder
